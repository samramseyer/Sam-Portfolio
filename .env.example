# Optional GitHub token — fixes rate limits and loads organization repos you belong to.
# Create a fine-grained token with read access to metadata and repository contents.
# Copy this file to .env and uncomment the line below:
# VITE_GITHUB_TOKEN=ghp_your_token_here
